# Flask-Tutorial

# Installation

  - ## Create an Virtual Environment
    ```
    $ python -m venv 'VIRTUAL ENVIRONMENT NAME'
    ```
  - ## Flask Installation

    ```
    $ pip install Flask
    ```
